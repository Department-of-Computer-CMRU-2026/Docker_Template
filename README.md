# คู่มือการติดตั้ง Docker สำหรับนักศึกษา

โปรเจกต์นี้จัดเตรียมสภาพแวดล้อมการพัฒนาด้วย Docker ซึ่งช่วยให้นักศึกษาหลายคนสามารถรัน Instance ของตัวเองแยกกันได้บนเซิร์ฟเวอร์เดียวกัน

## 🚀 เริ่มต้นใช้งานด่วน (Quick Start)

### 1. ตั้งค่า Environment
เปิดไฟล์ `.env` และตั้งค่า **รหัสนักศึกษา (Student ID)** และ **พอร์ต (Ports)** ของคุณ

**ตัวอย่างสำหรับนักศึกษา 66xxxxxx **:
```ini
# รหัสนักศึกษาของคุณ (ใช้ตั้งชื่อ Container)
STD_ID=66xxxxxx

# พอร์ตเว็บ (เข้าใช้งานผ่าน http://localhost:6056)
# รูปแบบ: 60 + เลข 2 หลักท้ายของรหัส
WEB_PORT=6xxx

# การตั้งค่าฐานข้อมูล
DB_CONNECTION=pgsql
# ชื่อโฮสต์ภายใน (ต้องตรงกับ std<รหัส>-db)
DB_HOST=std66xxxxxx-db
# พอร์ตฐานข้อมูล (สำหรับเชื่อมต่อด้วยโปรแกรมภายนอก เช่น DBeaver)
# รูปแบบ: 64 + เลข 2 หลักท้ายของรหัส (เชื่อมต่อผ่านพอร์ต 6456)
DB_PORT=6xxx

# ข้อมูลการเข้าใช้ฐานข้อมูล (กำหนดเองได้)
DB_DATABASE=db_name
DB_USERNAME=db_66xxxxxx
DB_PASSWORD=db_password
```

### 2. เริ่มการทำงานของ Containers
รันคำสั่ง Docker Compose:
```bash
docker-compose up -d
```

### 3. เข้าใช้งานระบบ
- **Web App**: `http://localhost:<WEB_PORT>` (เช่น `http://localhost:6xxx`)
- **Database**: Host: `localhost`, Port: `<DB_PORT>` (เช่น `6xxx`)

---

## 📂 รายละเอียดการตั้งค่า (Configuration Details)

### ตัวแปรในไฟล์ `.env`
| ตัวแปร | คำอธิบาย | ตัวอย่าง |
|---|---|---|
| `STD_ID` | รหัสนักศึกษาของคุณ ใช้สำหรับนำหน้าชื่อ Container | `66xxxxxx` |
| `WEB_PORT` | พอร์ตสำหรับเข้าใช้งานเว็บไซต์ผ่าน Browser | `6xxx` |
| `DB_HOST` | ชื่อโฮสต์ **ภายใน** Docker (ต้องตรงกับ `std<ID>-db`) | `std66xxxxxx-db` |
| `DB_PORT` | พอร์ต **ภายนอก** สำหรับเชื่อมต่อเครื่องมือจัดการฐานข้อมูล | `6xxx` |

### โครงสร้าง `docker-compose.yml`
ไฟล์นี้จะใช้ `STD_ID` ของคุณเพื่อสร้างชื่อ Resource ที่ไม่ซ้ำกัน:
- **Service**: `app` → ชื่อ Container: `std<ID>-app`
- **Service**: `db` → ชื่อ Container: `std<ID>-db`
- **Network**: `std<ID>-network`
- **Volume**: `std<ID>-db-data` (ข้อมูลฐานข้อมูลแยกกัน ไม่ซ้ำแน่นอน)

สิ่งนี้ช่วยให้ Container และข้อมูลของนักศึกษา A ไม่ชนกับของนักศึกษา B แม้จะรันบนเครื่องเดียวกัน
