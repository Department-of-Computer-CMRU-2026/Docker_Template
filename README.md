# คู่มือการติดตั้ง Docker สำหรับนักศึกษา

โปรเจกต์นี้จัดเตรียมสภาพแวดล้อมการพัฒนาด้วย Docker ซึ่งช่วยให้นักศึกษาหลายคนสามารถรัน Instance ของตัวเองแยกกันได้บนเซิร์ฟเวอร์เดียวกัน

## 🚀 เริ่มต้นใช้งานด่วน (Quick Start)

### 1. ตั้งค่า Environment
สร้างไฟล์ `.env` โดยคัดลอกเนื้อหาจาก `.env.test` และตั้งค่า **รหัสนักศึกษา (Student ID)** และ **พอร์ต (Ports)** ของคุณ

**ตัวอย่างสำหรับนักศึกษา 66xxxxxx**:
```ini
# Student Configuration
# Change '66xxxxxx' to your Student ID (e.g. 66123456)
# Web Port: Any unique port (e.g. 8000 + last 2 digits)
STUDENT_ID=66xxxxxx
STUDENT_NAME=66xxxxxx
STUDENT_PORT=6xxx
FORWARD_DB_PORT=6xxx

# Database Configuration
# Hostname: std{ID}-db
# Database Port: Any unique port (e.g. 5400 + last 2 digits)
DB_CONNECTION=pgsql
DB_HOST=std66xxxxxx-db
DB_PORT=6xxx
DB_DATABASE=db_name
DB_USERNAME=db_66xxxxxx
DB_PASSWORD=db_password
```

### 2. เริ่มการทำงานของ Containers
รันคำสั่ง Docker Compose:
```bash
docker-compose up -d --build
```
> [!NOTE]
> ควรใช้ flag `--build` ในครั้งแรกเพื่อให้แน่ใจว่า Docker Image ถูกสร้างขึ้นใหม่อย่างถูกต้อง

### 3. เข้าใช้งานระบบ
- **Web App**: `http://localhost:<STUDENT_PORT>` (เช่น `http://localhost:6xxx`)
- **Database**: Host: `localhost`, Port: `<FORWARD_DB_PORT>` (เช่น `6xxx`)

---

## 📂 รายละเอียดการตั้งค่า (Configuration Details)

### ตัวแปรในไฟล์ `.env`
| ตัวแปร | คำอธิบาย | ตัวอย่าง |
|---|---|---|
| `STUDENT_ID` | รหัสนักศึกษา | `66xxxxxx` |
| `STUDENT_NAME` | ชื่อที่ใช้สำหรับนำหน้าชื่อ Container (ปกติใช้รหัสนักศึกษา) | `66xxxxxx` |
| `STUDENT_PORT` | พอร์ตสำหรับเข้าใช้งานเว็บไซต์ผ่าน Browser (Nginx) | `6xxx` |
| `FORWARD_DB_PORT` | พอร์ต **ภายนอก** สำหรับเชื่อมต่อเครื่องมือจัดการฐานข้อมูล | `6xxx` |
| `DB_HOST` | ชื่อโฮสต์ **ภายใน** Docker (สำหรับการเชื่อมต่อระหว่าง Container) | `std66xxxxxx-db` |

### โครงสร้าง `docker-compose.yml`
ไฟล์นี้จะใช้ `STUDENT_NAME` ของคุณเพื่อสร้างชื่อ Resource ที่ไม่ซ้ำกัน:
- **Service**: `app` → ชื่อ Container: `${STUDENT_NAME}-smartclass-app`
- **Service**: `nginx` → ชื่อ Container: `${STUDENT_NAME}-smartclass-nginx`
- **Service**: `db` → ชื่อ Container: `${STUDENT_NAME}-smartclass-db`
- **Network**: `${STUDENT_NAME}-smartclass-net`
- **Volume**: `${STUDENT_NAME}-smartclass-db-data` (ข้อมูลฐานข้อมูลแยกกัน)
- **Volume**: `${STUDENT_NAME}-smartclass-storage-data` (ข้อมูลไฟล์อัปโหลด)

> [!WARNING]
> Container และข้อมูลของนักศึกษา A ไม่ชนกับของนักศึกษา B แม้จะรันบนเครื่องเดียวกัน ตราบใดที่ตั้งค่า `STUDENT_NAME` และ Port ไม่ซ้ำกัน
